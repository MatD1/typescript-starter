# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type ActivePeriodObject {
  end: Int
  start: Int
}

type AdminApiKey {
  createdAt: DateTime!
  enabled: Boolean!
  expiresAt: DateTime
  id: ID!
  name: String
  permissions: String
  rateLimitMax: Float
  remaining: Int
  requestCount: Int!
  start: String
  updatedAt: DateTime!
  userId: String!
}

type AdminApiKeyDetail {
  createdAt: DateTime!
  enabled: Boolean!
  expiresAt: DateTime
  id: ID!
  name: String
  permissions: String
  rateLimitMax: Float
  remaining: Int
  requestCount: Int!
  start: String
  updatedAt: DateTime!
  usage7d: [DailyUsageBucket!]!
  userId: String!
}

type AdminLogEntry {
  createdAt: DateTime!
  error: String
  id: ID!
  ipAddress: String
  keyId: String
  method: String!
  path: String!
  responseTimeMs: Int!
  statusCode: Int!
  userAgent: String
  userId: String
}

type AdminLogPage {
  data: [AdminLogEntry!]!
  nextCursor: String
  total: Int!
}

type AdminOverviewStats {
  activeUsers7d: Int!
  errorRate24h: Float!

  """Most-requested API path in last 24h"""
  topPath: String!
  totalRequests24h: Int!
}

type AdminUser {
  banned: Boolean!
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String!
  role: String!
  updatedAt: DateTime!
}

type AdminUserDetail {
  apiKeyCount: Int!
  banned: Boolean!
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String!
  requestCount7d: Int!
  role: String!
  updatedAt: DateTime!
}

"""Per-carriage composition and occupancy (TfNSW extension)"""
type CarriageDescriptorObject {
  """Predicted occupancy status at next departure"""
  departureOccupancyStatus: String

  """Whether this carriage has a luggage rack"""
  luggageRack: Boolean
  name: String

  """Current occupancy status of this carriage"""
  occupancyStatus: String

  """Position of this carriage within the consist (1-based)"""
  positionInConsist: Int!

  """Whether this is a designated quiet carriage"""
  quietCarriage: Boolean

  """Toilet availability: NONE | NORMAL | ACCESSIBLE"""
  toilet: String
}

type DailyUsageBucket {
  count: Int!
  date: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type DepartureObject {
  departureTimeEstimated: String
  departureTimePlanned: String
  destination: String

  """Line code from GTFS (e.g. T1, CCN)"""
  lineCode: String
  lineName: String
  platform: String

  """Route colour hex from GTFS (e.g. 009B77)"""
  routeColour: String
  stopId: String
  stopName: String
  transportMode: String
}

type DisruptionObject {
  activePeriods: [ActivePeriodObject!]!
  cause: String
  descriptionText: String
  effect: String
  headerText: String
  id: String!
  informedEntities: [InformedEntityObject!]!
  mode: String!

  """Severity level: UNKNOWN_SEVERITY | INFO | WARNING | SEVERE"""
  severityLevel: String

  """TTS (text-to-speech) version of the description"""
  ttsDescriptionText: String

  """TTS (text-to-speech) version of the header"""
  ttsHeaderText: String
  url: String
}

type EndpointStat {
  count: Int!
  path: String!
}

type GtfsIngestResult {
  modesIngested: [String!]!
  success: Boolean!
}

type GtfsRouteObject {
  mode: String
  routeColor: String
  routeId: String!
  routeLongName: String
  routeShortName: String
  routeType: Int
}

type GtfsStatus {
  lastIngest: String
  tableCounts: [GtfsTableCount!]!
}

type GtfsStopObject {
  lat: Float
  lon: Float
  mode: String
  stopCode: String
  stopId: String!
  stopName: String!
}

type GtfsStopTimeObject {
  arrivalTime: String
  departureTime: String
  dropOffType: Int
  id: String!
  mode: String
  pickupType: Int
  stopId: String!
  stopSequence: Int!
  tripId: String!
}

type GtfsTableCount {
  count: Int!
  table: String!
}

type GtfsTripObject {
  directionId: Int
  mode: String
  routeId: String
  serviceId: String
  tripHeadsign: String
  tripId: String!
}

type InformedEntityObject {
  agencyId: String
  directionId: Int
  routeId: String

  """GTFS route_type (0=tram, 1=metro, 2=rail, 3=bus, 4=ferry)"""
  routeType: Int
  stopId: String
  tripId: String
}

type KeyStat {
  count: Int!
  keyId: String!
  keyName: String
}

type LegObject {
  arrivalTimeEstimated: String
  arrivalTimePlanned: String
  departureTimeEstimated: String
  departureTimePlanned: String
  dest: LocationObject
  destination: String
  duration: Int

  """Line code from GTFS (e.g. T1, CCN)"""
  lineCode: String
  lineName: String
  origin: LocationObject

  """Route colour hex from GTFS (e.g. 009B77)"""
  routeColour: String
  transportation: String
  tripId: String
}

type LivePositionObject {
  bearing: Float

  """Per-carriage occupancy and amenity data"""
  consist: [CarriageDescriptorObject!]

  """INCOMING_AT | STOPPED_AT | IN_TRANSIT_TO"""
  currentStatus: String
  currentStopId: String
  currentStopSequence: Int
  latitude: Float!
  longitude: Float!
  occupancyStatus: String
  speed: Float
  timestamp: Int

  """TfNSW track direction: UP | DOWN"""
  trackDirection: String
}

type LiveStopTimeUpdateObject {
  """Arrival delay in seconds (positive = late)"""
  arrivalDelay: Int

  """Departure delay in seconds"""
  departureDelay: Int
  departureOccupancyStatus: String
  scheduleRelationship: String
  stopId: String
  stopSequence: Int
}

type LocationObject {
  id: String
  lat: Float
  lon: Float
  name: String
  type: String
}

type Mutation {
  adminDeleteApiKey(id: ID!): Boolean!
  adminDeleteUser(id: ID!): Boolean!
  adminFlushCache: Boolean!
  adminResetApiKeyUsage(id: ID!): AdminApiKey!
  adminTriggerGtfsIngest: GtfsIngestResult!
  adminUpdateApiKey(id: ID!, input: UpdateApiKeyInput!): AdminApiKey!
  adminUpdateUser(id: ID!, input: UpdateUserInput!): AdminUser!
}

type PaginatedApiKeys {
  data: [AdminApiKey!]!
  limit: Int!
  page: Int!
  total: Int!
}

type PaginatedRoutesObject {
  data: [GtfsRouteObject!]!
  hasNextPage: Boolean!
  limit: Int!
  offset: Int!
  total: Int!
}

type PaginatedStopTimesObject {
  data: [GtfsStopTimeObject!]!
  hasNextPage: Boolean!
  limit: Int!
  offset: Int!
  total: Int!
}

type PaginatedStopsObject {
  data: [GtfsStopObject!]!
  hasNextPage: Boolean!
  limit: Int!
  offset: Int!
  total: Int!
}

type PaginatedTripsObject {
  data: [GtfsTripObject!]!
  hasNextPage: Boolean!
  limit: Int!
  offset: Int!
  total: Int!
}

type PaginatedUsers {
  data: [AdminUser!]!
  limit: Int!
  page: Int!
  total: Int!
}

type Query {
  adminApiKey(id: ID!): AdminApiKeyDetail!
  adminApiKeys(enabled: Boolean, limit: Int, page: Int, userId: ID): PaginatedApiKeys!
  adminErrorLogs(cursor: String, from: String, limit: Int, to: String): AdminLogPage!
  adminGtfsStatus: GtfsStatus!
  adminHealth: SystemHealth!
  adminLogs(cursor: String, from: String, keyId: ID, limit: Int, method: String, path: String, statusCode: Int, to: String, userId: ID): AdminLogPage!
  adminMe: AdminUser!
  adminStatsEndpoints(from: String, limit: Int, to: String): [EndpointStat!]!
  adminStatsKeys(from: String, limit: Int, to: String): [KeyStat!]!
  adminStatsOverview: AdminOverviewStats!
  adminStatsUsage(from: String!, granularity: UsageGranularity!, to: String!): [UsageBucket!]!
  adminStatsUsers(from: String, limit: Int, to: String): [UserStat!]!
  adminUser(id: ID!): AdminUserDetail!
  adminUsers(limit: Int, page: Int, role: String, search: String): PaginatedUsers!

  """Get departure board for a stop."""
  departures(itdDate: String, itdTime: String, stopId: String, stopName: String): [DepartureObject!]!

  """Get current service disruptions and alerts."""
  disruptions(effect: String, mode: TransportMode): [DisruptionObject!]!

  """Search for stops and stations by name."""
  findStops(
    query: String!

    """any | coord | poi | stop"""
    type: StopFinderType
  ): [StopObject!]!

  """List GTFS static routes (paginated)."""
  gtfsRoutes(limit: Int, mode: TransportMode, offset: Int): PaginatedRoutesObject!

  """Total count of GTFS static routes."""
  gtfsRoutesCount(mode: TransportMode): Int!

  """List GTFS static stop times (paginated)."""
  gtfsStopTimes(limit: Int, offset: Int, stopId: String, tripId: String): PaginatedStopTimesObject!

  """List GTFS static stops (paginated)."""
  gtfsStops(limit: Int, mode: TransportMode, offset: Int): PaginatedStopsObject!

  """Total count of GTFS static stops."""
  gtfsStopsCount(mode: TransportMode): Int!

  """List GTFS static trips (paginated)."""
  gtfsTrips(limit: Int, offset: Int, routeId: String): PaginatedTripsObject!

  """Total count of GTFS static trips."""
  gtfsTripsCount(routeId: String): Int!

  """Find stations near a coordinate."""
  nearbyStations(lat: Float!, limit: Float, lon: Float!, radius: Float): [StationObject!]!

  """Find stops near a coordinate."""
  nearbyStops(lat: Float!, lon: Float!, radius: Float): [StopObject!]!

  """Plan a journey between two locations."""
  planTrip(
    calcNumberOfTrips: Int

    """lon:lat:EPSG:4326 (longitude first)"""
    destCoord: String
    destId: String
    destName: String
    itdDate: String
    itdTime: String

    """lon:lat:EPSG:4326 (longitude first)"""
    originCoord: String
    originId: String
    originName: String
    wheelchair: Boolean
  ): [TripResultObject!]!

  """Search for stations/stops by name."""
  searchStations(limit: Float, query: String!): [StationObject!]!

  """Get a station by stop ID."""
  stationById(stopId: String!): StationObject

  """
  Track a specific trip live. Returns vehicle position, delays, stop-time updates and vehicle amenity info for the given GTFS trip ID. Returns null if the vehicle is not yet active or the trip has ended. Pass the `tripId` field from a planned journey leg (planTrip query).
  """
  trackTrip(
    """Optional mode hint — improves lookup speed"""
    mode: TransportMode

    """GTFS trip ID from a planned leg"""
    tripId: String!
  ): TrackedTripObject

  """Get live trip updates. Optionally filter by transport mode."""
  tripUpdates(mode: TransportMode): [TripUpdateObject!]!

  """Get live vehicle positions. Optionally filter by transport mode."""
  vehiclePositions(mode: TransportMode): [VehiclePositionObject!]!
}

type StationObject {
  lat: Float
  locationType: Int
  lon: Float
  mode: String
  parentStation: String
  platformCode: String
  routes: [GtfsRouteObject!]
  stopCode: String
  stopId: String!
  stopName: String!
  wheelchairBoarding: Int
}

"""Type of results for stop finder search (v1 API type_sf)"""
enum StopFinderType {
  """All location types — use for free-text name search (e.g. "Wynyard")"""
  any

  """
  Coordinate lookup — query must be lon:lat:EPSG:4326. Use "any" for name search.
  """
  coord

  """Places of interest — restrictive. Use "any" for general name search."""
  poi

  """
  Stop ID only (e.g. 200060) — not for name search. Use "any" for name search.
  """
  stop
}

type StopObject {
  disassembledName: String
  id: String
  lat: Float
  lon: Float
  name: String
  transportMode: String
  type: String
}

type StopTimeUpdateObject {
  arrivalDelay: Int
  arrivalTime: Int

  """Predictive per-carriage occupancy at this stop (TfNSW extension 1007)"""
  carriagePredictiveOccupancy: [CarriageDescriptorObject!]
  departureDelay: Int

  """Occupancy status at this stop departure"""
  departureOccupancyStatus: String
  departureTime: Int
  scheduleRelationship: String
  stopId: String
  stopSequence: Int
}

type SystemHealth {
  checks: [SystemHealthCheck!]!
  healthy: Boolean!
}

type SystemHealthCheck {
  latencyMs: Int!
  name: String!
  status: String!
}

type TrackedTripObject {
  airConditioned: Boolean

  """Overall trip delay in seconds (positive = late)"""
  delay: Int

  """Line code from GTFS (e.g. T1, CCN)"""
  lineCode: String

  """Transport mode this vehicle belongs to"""
  mode: String!
  performingPriorTrip: Boolean

  """Live GPS position and status. Null if vehicle is not yet active."""
  position: LivePositionObject

  """Route colour hex from GTFS (e.g. 009B77)"""
  routeColour: String
  routeId: String

  """Overall trip schedule relationship"""
  scheduleRelationship: String

  """Real-time updates for upcoming stops on this trip"""
  stopTimeUpdates: [LiveStopTimeUpdateObject!]

  """GTFS trip ID — matches the tripId on a planned LegObject"""
  tripId: String!
  vehicleId: String

  """Vehicle label / fleet set number"""
  vehicleLabel: String
  vehicleModel: String

  """0=unknown 1=accessible 2=not accessible"""
  wheelchairAccessible: Int
}

"""NSW transport network mode"""
enum TransportMode {
  """Sydney metropolitan buses"""
  buses

  """Sydney Ferries"""
  ferries

  """Intercity / regional rail (filtered from Sydney Trains feed by route)"""
  intercity

  """Light rail (CBD, Inner West, Parramatta)"""
  lightrail

  """Sydney Metro rapid transit"""
  metro

  """NSW TrainLink long-distance trains"""
  nswtrains

  """Sydney Trains suburban network"""
  sydneytrains
}

type TripResultObject {
  duration: Int
  interchanges: Int
  legs: [LegObject!]!
}

type TripUpdateObject {
  """Overall trip delay in seconds"""
  delay: Int
  directionId: Int

  """Line code from GTFS (e.g. T1, CCN)"""
  lineCode: String
  mode: String!

  """Route colour hex from GTFS (e.g. 009B77)"""
  routeColour: String
  routeId: String
  scheduleRelationship: String
  startDate: String
  startTime: String
  stopTimeUpdates: [StopTimeUpdateObject!]!
  timestamp: Int
  tripId: String!
  vehicleId: String

  """Vehicle label / set number"""
  vehicleLabel: String
}

input UpdateApiKeyInput {
  enabled: Boolean
  permissions: String
  rateLimitMax: Int
}

input UpdateUserInput {
  banned: Boolean
  role: String
}

type UsageBucket {
  count: Int!
  errors: Int!
  timestamp: String!
}

enum UsageGranularity {
  DAY
  HOUR
}

type UserStat {
  count: Int!
  userId: String!
  userName: String!
}

type VehiclePositionObject {
  airConditioned: Boolean
  bearing: Float
  congestionLevel: String

  """Per-carriage composition with occupancy and amenity data"""
  consist: [CarriageDescriptorObject!]
  currentStatus: String
  currentStopId: String
  currentStopSequence: Int
  directionId: Int
  latitude: Float!

  """Line code from GTFS (e.g. T1, CCN)"""
  lineCode: String
  longitude: Float!
  mode: String!
  occupancyStatus: String
  odometer: Float
  performingPriorTrip: Boolean

  """Route colour hex from GTFS (e.g. 009B77)"""
  routeColour: String
  routeId: String
  specialVehicleAttributes: Int
  speed: Float
  startDate: String
  startTime: String
  timestamp: Int

  """Track direction: UP | DOWN"""
  trackDirection: String
  tripId: String
  tripScheduleRelationship: String
  vehicleId: String!

  """Vehicle label / set number"""
  vehicleLabel: String
  vehicleModel: String

  """0=unknown, 1=accessible, 2=not accessible"""
  wheelchairAccessible: Int
}